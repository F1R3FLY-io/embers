new rl(`rho:registry:lookup`), oslfsCh in {
    rl!({{ env_uri }}, *oslfsCh) |
    for(@(_, oslfs) <- oslfsCh) {
        @oslfs!("save", {{ id }}, {{ version }}, {{ created_at }}, {{ name }}, {{ description }}, {{ query }})
    }
}
