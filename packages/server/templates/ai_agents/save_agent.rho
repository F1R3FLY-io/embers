new rl(`rho:registry:lookup`), agentsCh in {
    rl!({{ env_uri }}, *agentsCh) |
    for(@(_, agents) <- agentsCh) {
        @agents!("save", {{ id }}, {{ version }}, {{ created_at }}, {{ name }}, {{ shard }}, {{ logo }}, {{ code }})
    }
}
