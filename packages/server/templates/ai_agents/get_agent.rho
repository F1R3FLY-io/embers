new ret, rl(`rho:registry:lookup`), agentsCh in {
    rl!({{ env_uri }}, *agentsCh) |
    for(@(_, agents) <- agentsCh) {
        @agents!("get", {{ address }}, {{ id }}, {{ version }}, *ret)
    }
}
