new rl(`rho:registry:lookup`), agentsCh in {
    rl!({{ env_uri }}, *agentsCh) |
    for(@(_, agents) <- agentsCh) {
        @agents!("create", {{ id }}, {{ version }}, {{ name }}, {{ shard }}, {{ code }})
    }
}
