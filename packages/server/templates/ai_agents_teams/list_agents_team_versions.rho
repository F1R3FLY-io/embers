new ret, rl(`rho:registry:lookup`), agentsTeamsCh in {
    rl!({{ env_uri }}, *agentsTeamsCh) |
    for(@(_, agentsTeams) <- agentsTeamsCh) {
        @agentsTeams!("listVersions", {{ address }}, {{ id }}, *ret)
    }
}
