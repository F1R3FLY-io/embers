new rl(`rho:registry:lookup`), agentsTeamsCh in {
    rl!({{ env_uri }}, *agentsTeamsCh) |
    for(@(_, agentsTeams) <- agentsTeamsCh) {
        @agentsTeams!("create", {{ id }}, {{ version }}, {{ name }}, {{ shard }}, {{ created_at }}, {{ graph }})
    }
}
