new rl(`rho:registry:lookup`),
    deployerId(`rho:rchain:deployerId`),
    agentsTeamsCh
in {
    rl!({{ env_uri }}, *agentsTeamsCh) |
    for(@(_, agentsTeams) <- agentsTeamsCh) {
        @agentsTeams!("create", *deployerId, {{ id }}, {{ version }}, {{ name }}, {{ shard }}, {{ graph }})
    }
}
