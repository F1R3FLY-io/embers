{%- match from -%}
    {%- when From::Input -%}
return!(input)
    {%- when From::Channel(from) -%}
for(@returnValue <- {{ from }}) {
    {{ from }}!(returnValue) |
    return!(returnValue)
}
{%- endmatch -%}
